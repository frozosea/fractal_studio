# Browser talks only to Platform through Next reverse proxy.
NEXT_PUBLIC_PLATFORM_API_URL=/platform
# Server-only rewrite destination. Docker Compose overrides this with http://api:8000.
PLATFORM_INTERNAL_URL=http://localhost:8000
# Never inject FSD_COMPUTE_SERVICE_KEY into a browser build.
